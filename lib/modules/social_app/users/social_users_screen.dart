import 'package:flutter/material.dart';

class SocialUsers extends StatelessWidget {
  const SocialUsers({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children:
      const [
        Center(
          child: Text(
            'Social Users'
          ),
        )
      ],
    );
  }
}
